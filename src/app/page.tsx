import Providers from "@/redux/provider";
import React from "react";

type Props = {};

const page = (props: Props) => {
	return (
		<Providers>
			<div>Index</div>
		</Providers>
	);
};

export default page;
