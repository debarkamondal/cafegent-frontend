export interface errorType {
	status?: string;
	message?: string;
}

export interface menuItemType {
	id: string;
	sk: string;
	name: string;
	price: string;
	tag: string;
	description: string;
	image: string;
}
